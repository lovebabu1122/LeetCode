class Solution {
    public int strStr(String haystack, String needle) {
        int index = haystack.indexOf(needle);
        // int i = 0;
        // while (i < haystack.length && haystack.charAt(i) ==needle.charAt(i)) {
        //     i++;
        // }
        return index;
    }
}